<% if current_user %>
  <nav class="navbar navbar-inverse" role="navigation">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#users/<%=current_user.id%>">TrakMyRun</a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li class="active">
            <a href="#users/<%=current_user.id%>" class="show-icon">
              Home
            </a>
          </li>
         <li>
          <a href="#users">
            <span class="glyphicon glyphicon-flash icon-hover"></span>
            The hub
          </a>
        </li>
          
          <li>
            <a href="#users/<%=current_user.id%>/routes/show" class="show-icon">
              <span class="glyphicon glyphicon-map-marker icon-hover"></span> Create Route
            </a>
          </li>

          <li>
            <a href="#posts/new">
              <span class="glyphicon glyphicon-pencil icon-hover"></span> Log Workout
            </a>
          </li>
          <li>
            <a href="#users/<%=current_user.id%>/dashboard">
              <span class="glyphicon glyphicon-th icon-hover"></span>
              Dashboard
            </a>
          </li>
          <li>
            <a href="">
              Leaderboard
            </a>
          </li>  
        </ul>
        <!-- <form class="navbar-form navbar-left" role="search">
          <div class="form-group">
            <input type="text" class="form-control" placeholder="Search">
          </div>
          <button type="submit" class="btn btn-default">Submit</button>
        </form> -->
        <ul class="nav navbar-nav navbar-right">
          
          <li><a href="#users/<%=current_user.id%>" 
                class="glyphicon glyphicon-home icon-hover blue"></a></li>
          <li class="dropdown">
            <a href="" class="dropdown-toggle" data-toggle="dropdown"><%=current_user.username%><span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><a href="">Edit</a></li>
              <li><a href="">Settings</a></li>
              <li><a href="">Goals</a></li>
              <li class="divider"></li>
              <li><%= link_to "Log Out", sessions_url, method: :delete%></li>
            </ul>
          </li>

        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>

  <!-- IMAGE UPLOAD FORM -->
  <div class="col-md-12 text-center " id="filepicker-form">
    <form class="form col-md-4 col-md-offset-4" action="<%=user_url(current_user)%>" method="post"> 

      <div class="form-group">

        <label for="image"><h1>Upload Image</h1></label> 
        <br>
        <input type="hidden" name="_method" value="PATCH">
        <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">
        <input type='filepicker' class="form-control" name="users[image_url]">
        <br>
        <div class="row">
          <div class="col-md-6">
            <button class="btn btn-primary form-control">Submit</button>
          </div>

          <div class="col-md-6">
            <button class="btn btn-danger form-control col-md-3 close-form" data-form-id="#filepicker-form">Cancel</button>
          </div>

        </div>
      </div>
    </form>
  </div>

</div>

<% else %>
<div class="row">
  <div class="col-md-3 pull-right">
    <% if !current_page?(new_sessions_path) %>
      <a href="<%=new_sessions_path%>" class="visitor-link">Sign In</a>
    <% else %>
      <a href="<%=new_user_path%>" class="visitor-link">Sign Up</a>
    <% end %>
  </div>
</div>
<% end %>


  






